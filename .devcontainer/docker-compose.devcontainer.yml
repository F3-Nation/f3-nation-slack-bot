# This override compose file is no longer used by devcontainer.json.
# Root compose.yml now directly sets app.build.dockerfile to .devcontainer/Dockerfile.dev.
# You can delete this file if you prefer; keeping an empty services map keeps it a valid Compose file.
services: {}